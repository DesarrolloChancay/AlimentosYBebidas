# Archivo: .env.example
# Copia este archivo como .env y configura tus valores locales

# Entorno de ejecución (development/production)
FLASK_ENV=development

# Clave secreta para sesiones de Flask
SECRET_KEY=tu_clave_secreta_muy_segura_aqui

# Configuración de base de datos MySQL
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_contraseña_mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=alimentosybebidas

# En producción (Render), estas variables se configuran en el dashboard de Render
# No es necesario incluirlas en .env para producción